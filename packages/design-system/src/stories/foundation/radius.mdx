import { Meta } from '@storybook/blocks';
import { theme } from '$lib/theme/theme';

<Meta
  title="foundation/Radius"
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true }
    }
  }}
/>

# Radiuses

Use `--<variableName>` in css. Within the current theme, the value will be `theme.radius.<variableName>`.

<div className="story-box">
  <div>
    <code>radiusMinimal</code>
  </div>
  <div class="code">{theme.radius.radiusMinimal}</div>
  <div className="shape" style={{ borderRadius: theme.radius.radiusMinimal }} />
  <div className="square" style={{ borderRadius: theme.radius.radiusMinimal }} />
  <div>
    <code>radiusMiddle</code>
  </div>
  <div class="code">{theme.radius.radiusMiddle}</div>
  <div className="shape" style={{ borderRadius: theme.radius.radiusMiddle }} />
  <div className="square" style={{ borderRadius: theme.radius.radiusMiddle }} />
  <div>
    <code>radiusRounded</code>
  </div>
  <div class="code">{theme.radius.radiusRounded}</div>
  <div className="shape" style={{ borderRadius: theme.radius.radiusRounded }} />
  <div className="square" style={{ borderRadius: theme.radius.radiusRounded }} />
  <div>
    <code>radiusFull</code>
  </div>
  <div class="code">{theme.radius.radiusFull}</div>
  <div className="shape" style={{ borderRadius: theme.radius.radiusFull }} />
  <div className="square" style={{ borderRadius: theme.radius.radiusFull }} />
</div>

<style>{`
  .story-box {
    display: grid;
    grid-template-columns: 150px 100px 150px 1fr;
    align-items: center;
    gap: 30px;
    margin: 30px 0;
    font-family: Arial
  }
  code, .code {
    padding: 4px 10px;
    border-radius: 3px;
    margin-right: 20px;
    display: inline;
    font-family: 'SFMono-Regular', Consolas, 'Liberation Mono', Menlo, Courier, monospace;
    font-size: 14px;
  }
  code {
    border: 1px solid #ccc;
    background-color: #F4F4F4;
    font-size: 16px;
  }
  .shape, .square {
    width: 140px;
    height: 40px;
    background-color: var(--color-interactionHighlightPrimary);
  }
  .square {
    width: 40px;
  }
`}</style>
