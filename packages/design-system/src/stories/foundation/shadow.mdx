import { Meta } from '@storybook/blocks';
import { theme } from '$lib/theme/theme';

<Meta
  title="foundation/Shadow"
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true }
    }
  }}
/>

# Spacing

Use `--<variableName>` in css. Within the current theme, the value will be `theme.shadow.<variableName>`.

<div className="story-box">
  <div>
    <code>shadowNone</code>
  </div>
  <div class="code">{theme.shadow.shadowNone}</div>
  <div className="shape" style={{ boxShadow: theme.shadow.shadowNone }} />
  <div>
    <code>shadowXS</code>
  </div>
  <div class="code">{theme.shadow.shadowXS}</div>
  <div className="shape" style={{ boxShadow: theme.shadow.shadowXS }} />
  <div>
    <code>shadowMD</code>
  </div>
  <div class="code">{theme.shadow.shadowMD}</div>
  <div className="shape" style={{ boxShadow: theme.shadow.shadowMD }} />
  <div>
    <code>shadowLG</code>
  </div>
  <div class="code">{theme.shadow.shadowLG}</div>
  <div className="shape" style={{ boxShadow: theme.shadow.shadowLG }} />
  <div>
    <code>shadowXL</code>
  </div>
  <div class="code">{theme.shadow.shadowXL}</div>
  <div className="shape" style={{ boxShadow: theme.shadow.shadowXL }} />
  <div>
    <code>shadow2XL</code>
  </div>
  <div class="code">{theme.shadow.shadow2XL}</div>
  <div className="shape" style={{ boxShadow: theme.shadow.shadow2XL }} />
  <div>
    <code>shadowInner</code>
  </div>
  <div class="code">{theme.shadow.shadowInner}</div>
  <div className="shape" style={{ boxShadow: theme.shadow.shadowInner }} />
</div>

<style>{`
  .story-box {
    display: grid;
    grid-template-columns: 100px 420px 1fr;
    align-items: center;
    grid-row-gap: 60px;
    grid-column-gap: 40px;
    margin: 30px 0;
    font-family: var(--typography-fontFamilySecondary);
  }
  code, .code {
    padding: 4px 10px;
    border-radius: 3px;
    margin-right: 20px;
    display: inline;
    font-family: 'SFMono-Regular', Consolas, 'Liberation Mono', Menlo, Courier, monospace;
    font-size: 14px;
  }
  code {
    border: 1px solid #ccc;
    background-color: #F4F4F4;
    font-size: 16px;
  }
  .shape {
    width: 100px;
    height: 100px;
    background-color: white;
    border:1px solid #faf9f9;
  }
`}</style>
